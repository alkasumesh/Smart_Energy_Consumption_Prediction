Week 5 – LSTM Model Training

Energy consumption data was scaled using MinMaxScaler.
Sliding window sequences were created with 24 time steps.
An LSTM model was built with two stacked LSTM layers and
dropout for regularization. The model was trained using
Adam optimizer and early stopping.
